// here we're going to have an abstract function using joi for validating user input

// https://github.com/hapijs/joi


const Joi = require('joi');

module.exports = {
    validateBody: (schema) => {

    },
    schemas: {
        
    }
}
